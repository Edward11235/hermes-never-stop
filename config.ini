[watchdog]
# How long without session file changes before sending message (seconds)
# Default: 120 (2 minutes)
timeout = 30

# How often to check for session file changes (seconds)
# Default: 30
interval = 10

# The text to send when Hermes is stuck
# This message will be typed into the terminal via xdotool
message = continue
